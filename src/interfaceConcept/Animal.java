package interfaceConcept;


//SAM/functional interface [ also there are 2 more type of interface Marker and Normal
public interface Animal {
    String name="ronny";            //variable inside interface always be final& static, so its should be intialized.
    void speak();                 //methods are always abstract & public
}
