package interfaceConcept;

public class Dog implements Animal {      //this class implements interface then must define method present inside interface.

    public void speak(){
        System.out.println("dog always speak");
    }
}